# 23.04.2026 Progress

## Newly completed
- Fast-forward merged teammate `main` into local `main` (now based on `4e9b0b60d`).
- Validated `adaptive_muon` on the 1-GPU softmax ClimbMix smoke: job `1929341`, first successful step `1`, final loss `5.284095`, final `92078.3` tokens/s/GPU.
- Validated `adaptive_muon` on the 1-GPU GDN ClimbMix smoke after a launcher fix: job `1929351`, first successful step `1`, final loss `4.995165`, final `44783.9` tokens/s/GPU.
- Resolved `_research/launch/install_python_deps.sh` so it keeps teammate dependency validation logic and the known-good FLA pins (`fla-core==0.4.2`, `flash-linear-attention==0.4.2`).

## Teammate already solved
- Deterministic FineWeb-Edu preparation and conversion to Megatron format with LLaMA-2 tokenizer inputs.
- New 350M softmax and GDN launcher paths for FineWeb-Edu/LLaMA-2.
- A merged DeltaNet baseline path (`delta_net.py` plus launcher/spec wiring).

## Muon notes
- Softmax+Muon initially failed in job `1929331` because the 1-GPU smoke launcher used `--use-distributed-optimizer`, which pushed Muon onto the layer-wise distributed path. The quick Muon smoke launchers now stay on the plain single-rank optimizer path.
- GDN+Muon initially failed in job `1929347` with an invalid reshape in Muon's QKV split logic. The GDN Muon launchers now pass `--muon-no-split-qkv`.
- Current conclusion: Muon works correctly for the tiny Clariden bring-up runs; the remaining fixes were launcher-path issues, not core optimizer instability.

## Merge / conflict decisions
- Preferred teammate's LLaMA-2 tokenizer direction for FineWeb-Edu and new 350M launchers.
- Preserved the exact FLA dependency fix while keeping teammate install validation logic.
- Preserved Muon-specific launcher constraints: no `--overlap-param-gather` for Muon, and no QKV split on the GDN Muon launchers.

## Still open
- Materialize the FineWeb-Edu Megatron prefix on cluster once the shared `LLAMA2_TOKENIZER_MODEL` path is agreed.
- Run a longer AdamW vs Muon GDN comparison after the 300M GDN AdamW launcher is exercised.
- Start the CLER residual-stream implementation with flags default-off.

## Quick Results Plots
- Parsed local quick-run logs and generated summary plots under `tracker/week1/plots/attention_loss_curves.png` and `tracker/week1/plots/optimizer_loss_curves.png`.
- Detailed metrics and run notes are recorded in `tracker/week1/23_04_2026_results.md`.
